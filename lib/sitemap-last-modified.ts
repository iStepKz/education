/** Fixed dates for sitemap entries (avoid regenerating lastmod on every request). */
export const sitemapLastModified = {
  home: new Date("2026-09-22"),
  aiVibeCodingAstana: new Date("2026-09-22"),
} as const;
